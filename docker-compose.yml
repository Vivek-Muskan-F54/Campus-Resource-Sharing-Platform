services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: campus_share
      MYSQL_ROOT_PASSWORD: Vivekmuskan@54#$
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
volumes:
  mysql_data:
